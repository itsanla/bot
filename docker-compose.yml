services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: itsanla-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/bot.db
    volumes:
      - ./data:/app/data
